import sys
import math

x = float(0)
y = float(0)

def euc(a,b):
    d = 0

    for len()
    return math.sqrt((x-a)**2 + (y-b)**2)

  

for idx, x in enumerate(sys.stdin):

    if idx == 0:
        N = int(x)
        print(0)
        continue
    x = x.split()

    print(x)
    # print(euc(float(x[0]), float(x[1])))

    if idx == N:
        break


print(points)



    